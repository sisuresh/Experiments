# Protocol 28 — CAP-0085 (Externally Managed Contract Executables)

## Release scope

- Protocol number: 28
- CAP: **CAP-0085** — Externally managed contract executables
  (https://github.com/stellar/stellar-protocol/blob/master/core/cap-0085.md)
- CAPs dropped: (none)
- XDR feature flag: **`CAP_0085_EXECUTABLE_REF`** — already authored upstream
  by the CAP author. The canonical `.x` change is
  **stellar/stellar-xdr#308** (dmkozh, branch `cap85` on his fork), head SHA
  `76218a994f8c5ba752cba368080fb2f89843ad3c`. **Do NOT open a stellar-xdr PR
  and do NOT author the `.x` change yourself** — pin everything to that SHA.
- stellar-xdr commit for all regens: `76218a994f8c5ba752cba368080fb2f89843ad3c`.
  It is main (`8521b97b…`) + exactly the two cap85 commits (verified
  `behind_by: 0`), so regens at this SHA keep CAP-0083's gates intact.
  Verify with:
      gh api "repos/stellar/stellar-xdr/contents/Stellar-contract.x?ref=76218a994f8c5ba752cba368080fb2f89843ad3c" \
        --jq .content | base64 -d | grep CAP_0085_EXECUTABLE_REF

### Flag-token trap (read twice)

The CAP document's XDR diff uses a bare `#ifdef CAP_0085` — **that is stale**.
PR #308 is authoritative and the token is `CAP_0085_EXECUTABLE_REF`. Use it
VERBATIM everywhere downstream: rs-stellar-xdr / rs-soroban-env cargo feature
`cap_0085_executable_ref`, go/js `XDR_FEATURES` entry `CAP_0085_EXECUTABLE_REF`.
dmkozh's env draft (below) uses a bare `cap_0085` cargo feature — rename it
when adapting his branch. A mismatched token silently strips the new types
from generated code (lessons.md, stellar-xdr section).

## XDR change (what #308 adds — all in `Stellar-contract.x`, gated)

- `SCValType`: new arm `SCV_EXECUTABLE_TAG = 22`, `SCVal` payload
  `SCString executable_tag`.
- `ContractExecutableType`: new arm `CONTRACT_EXECUTABLE_EXTERNAL_REF = 2`.
- New `struct ContractExecutableExternalRef { SCAddress executable_owner;
  SCString tag; }` + matching `ContractExecutable` union arm.

## Targets (dep order, top-down)

- /Users/siddharthsuresh/dev/rs-stellar-xdr
- /Users/siddharthsuresh/dev/rs-soroban-env
- /Users/siddharthsuresh/dev/rs-soroban-sdk
- /Users/siddharthsuresh/dev/stellar-core
- /Users/siddharthsuresh/dev/go-stellar-sdk
- /Users/siddharthsuresh/dev/stellar-horizon
- /Users/siddharthsuresh/dev/stellar-rpc
- /Users/siddharthsuresh/dev/js-stellar-base
- /Users/siddharthsuresh/dev/js-stellar-sdk
- /Users/siddharthsuresh/dev/js-stellar-xdr-json
- /Users/siddharthsuresh/dev/stellar-laboratory
- /Users/siddharthsuresh/dev/docker-stellar-core-horizon

## Notes — read carefully before planning

### HARD CONSTRAINTS (operator mandate — overrides contract/lessons if they conflict)

1. **Push NOTHING outside the operator's forks.** Every `git push` in this run
   targets a `sisuresh/*` fork remote (the operator's GitHub user is
   `sisuresh`), and only the `p28-cap-0085` release branch. Never push any
   ref to `stellar/*` or `dmkozh/*` — even where the token HAS write access
   (it has push/maintain on stellar/stellar-core; that access must not be
   used). PRs are cross-fork drafts against `stellar/*` with
   `--head sisuresh:p28-cap-0085` — the PR object is the only thing that
   appears upstream; no upstream branch is ever created or modified.
2. **Merge NOTHING.** No `gh pr merge`, no auto-merge, no merge queue, no
   marking ready-for-review — for our PRs, dmkozh's PRs, or anyone else's.
   Every PR this run opens stays a **draft** for the entire run; merging is a
   human decision afterwards.
3. **Modify NOTHING on other people's PRs** (#308, #1703): no pushes, no
   edits, no closing. Referencing them in our PR descriptions is fine.

### This is a host/Soroban CAP with an AUTHORITATIVE upstream draft — build ON it, don't reimplement

- The CAP author has a comprehensive host implementation in flight:
  **stellar/rs-soroban-env#1703** ("CAP-85 draft", branch `exec_ref` on
  dmkozh's fork), head SHA `72bda35c715772c5f7df69b6264cd5f6248f9c94`.
  It already covers: `env.json` (3 new host fns), the new
  `ExecutableTagObject` host object, storage put/del validation, auth-context
  `ContractExecutable` enum update, TTL-extension updates, frame/lifecycle
  changes, tests (incl. `test/executable_tag.rs`) and observation regens.
- **Do NOT reimplement from scratch and do NOT open a competing
  implementation.** Fetch his branch (`git fetch origin pull/1703/head` in the
  rs-soroban-env checkout), branch `p28-cap-0085` from latest `main`, and merge
  his commits in **locally** (preserve his authorship — merge/cherry-pick,
  don't squash-copy; the result is pushed only to OUR fork, #1703 itself is
  untouched). Then productionize what makes his draft unbuildable downstream:
  1. His `Cargo.toml` patches `stellar-xdr = { path = "../rs-stellar-xdr" }`
     (a local-path `[patch.crates-io]`) — replace with a git+rev workspace dep
     pointing at THIS run's rs-stellar-xdr SPIKE head.
  2. His `features = ["cap_0085"]` — rename to `cap_0085_executable_ref` and
     wire per lessons.md: per-CAP **leaf** feature in each crate
     (common/host/guest), aggregated by `next`, code gated on the cap feature,
     `min_supported_protocol: 28` on all three host fns.
- Gap-check the merged result against the CAP spec and fill only what's truly
  missing (flag anything you add for human review in the PR description):
  `put_contract_data` validates persistent durability + existing-Wasm hash for
  tag keys / `del_contract_data` panics; `create_external_ref_contract`,
  `update_current_contract_executable_ref`, `create_executable_tag` semantics;
  `extend_*_ttl` variants (incl. the v2 scopes) extending reference entry +
  referenced code; `get_address_executable` resolving the ref to a Wasm hash;
  `InvokeHostFunctionOp` `CREATE_CONTRACT`/`CREATE_CONTRACT_V2` handling of the
  new executable variant; `executable_update` system event carrying the new
  enum variant.
- Cross-link #1703 prominently and state that the PR is based on dmkozh's
  draft.

### rs-stellar-xdr — THIS run authors it (no upstream PR exists)

- No CAP-0085 PR exists on stellar/rs-stellar-xdr (verified). Bump the `xdr/`
  git submodule to `76218a994f8c5ba752cba368080fb2f89843ad3c`, add the
  `cap_0085_executable_ref` feature in `Cargo.toml`, `make generate`, commit.
- The pinned SHA lives on dmkozh's fork but is fetchable from the base repo:
  in the submodule, `git fetch origin pull/308/head` (verified: the contents
  API and raw.githubusercontent both serve this SHA from stellar/stellar-xdr).
- Lessons.md exception applies: run ONLY `make generate` locally — never the
  feature-powerset build/test; CI runs the matrix.

### Fix routing under the fork-only constraint

- During the watch phase, any fix that logically belongs in #308 or #1703
  goes into OUR SPIKE layer that pins them (rs-stellar-xdr, or our
  rs-soroban-env branch) — see HARD CONSTRAINTS: dmkozh's branches are never
  touched. If the canonical #308 XDR itself is wrong, ESCALATE — don't work
  around it silently.

### Coexisting p28 SPIKE stacks — keep them separate

- CAP-0083 and CAP-0084 stacks are open and unmerged (e.g. stellar-xdr#307,
  rs-soroban-env#1700, core#5337, go-sdk#5961, horizon#203, rpc#837,
  js-stellar-base#981…). This run's `p28-cap-0085` stack is INDEPENDENT:
  branch from the base branches, not from the cap-0084 branches.
- Regens enable ONLY the flags already enabled on the base branch **plus**
  `CAP_0085_EXECUTABLE_REF`. Do NOT add `CAP_0084_MUXED_CONTRACT` (that's
  #5961's stack), and do NOT drop existing flags (go-stellar-sdk
  `protocol-next` already has `XDR_FEATURES ?= CAP_0083` and the
  `xfile preprocess` step wired — extend the list, keep the plumbing).
- Merge conflicts between the p28 stacks are a human concern at merge time —
  do not try to combine them.

### stellar-core

- `master` is still protocol 27 (`Config::CURRENT_LEDGER_PROTOCOL_VERSION = 27`,
  verified) and has only `p21`–`p27` soroban submodules — CAP-0084's core PR
  (#5337) adds its own p28 but is unmerged. Our SPIKE therefore includes the
  mechanical p28 bump itself: `CURRENT_LEDGER_PROTOCOL_VERSION = 28`, a named
  constant in `src/util/ProtocolVersion.h` (e.g.
  `EXTERNAL_EXECUTABLE_REF_PROTOCOL_VERSION = V_28`), and a **new
  `src/rust/soroban/p28` submodule** pointing at OUR rs-soroban-env SPIKE head
  (lessons.md: create p<N>, NEVER bump p27).
- The `InvokeHostFunctionOp` CREATE_CONTRACT handling lives in the host and
  arrives via the p28 submodule — core-side work is wiring, version gating,
  tests, and the expected `--check-test-tx-meta` baseline re-record.
- Match the previous protocol's `-vnext` image/deb variant and wait for the
  core artifact whose commit matches the core PR HEAD (see lessons.md).

### Go services (protocol-next) + JS

- go-stellar-sdk: base `protocol-next`; `XDR_COMMIT` →
  `76218a994f8c5ba752cba368080fb2f89843ad3c`; append
  `CAP_0085_EXECUTABLE_REF` to `XDR_FEATURES`; `make xdr`, commit Makefile +
  `.x` + generated files.
- horizon: base `protocol-next`; regen via the SDK pin. No semantic
  `contractevents` work expected: CAP-0085 does not change SAC transfer/mint
  events; only the `executable_update` system event gains a new
  `ContractExecutable` representation. Note that for the reviewer, don't
  build bespoke handling. Check `MaxSupportedProtocolVersion`
  (internal/ingest/main.go) — bump to 28 only if the base branch hasn't.
- stellar-rpc: base `protocol-next`; Go side regen; Rust side (preflight)
  pins rs-soroban-env + rs-stellar-xdr → OUR SPIKE heads.
- JS repos + laboratory: regen from
  `76218a994f8c5ba752cba368080fb2f89843ad3c` with the CAP flag;
  js-stellar-xdr-json pins OUR rs-stellar-xdr SPIKE. Base branches: default
  branch per repo (cap-0084 precedent: js-stellar-base PR based on `master`);
  confirm per repo.
- quickstart (docker-stellar-core-horizon): bump refs in `images.json`;
  blocked on the core `-vnext` artifact — a red caused by a missing artifact
  is SKIP, not a fix.

### Base branches (per-layer — lessons.md "Base branch by layer")

- Feature-gated layers → default branch: rs-stellar-xdr, rs-soroban-env,
  rs-soroban-sdk → `main`; stellar-core → `master`.
- Downstream Go services → `protocol-next`: go-stellar-sdk, stellar-horizon,
  stellar-rpc.
- JS + quickstart: confirm per repo.

### Pins that must NOT change

- Any unrelated Go / Rust / JS module bumps.
- Existing `XDR_FEATURES` entries on the base branches (extend, never drop).

### PR conventions

- Branch name (all repos): `p28-cap-0085`, pushed ONLY to the fork. Title:
  `Protocol 28 (CAP-0085)`, prefixed `SPIKE:`, opened as **draft** — and it
  STAYS draft (see HARD CONSTRAINTS: this run never marks ready or merges).
- Cross-repo SPIKE pins reference the FORK, not upstream refs: Cargo
  `git = "https://github.com/sisuresh/<repo>", rev = <SPIKE head>`,
  go.mod `replace` → fork pseudo-versions, npm git-URL/tarball deps — so
  downstream builds resolve without anything existing upstream.
- Cross-link the chain in each PR description, plus stellar-xdr#308 and
  rs-soroban-env#1703 as the canonical upstream references.
