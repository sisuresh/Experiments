# Protocol 28 — CAP-0083 only, on protocol-next

## Release scope

- Protocol number: 28
- CAPs enabled this release: **CAP-0083** (`STELLAR_VALUE_EMPTY_TX_SET`)
- CAPs dropped: (none)
- Canonical `stellar-xdr` commit: 8521b97ba0e8e71d746bf3bd73def92110e6a21f with the CAP_0083 ifdef feature enabled.
Verify with:
    gh api repos/stellar/stellar-xdr/contents/Stellar-ledger.x?ref=<sha> \
      | jq -r .content | base64 -d | grep STELLAR_VALUE_EMPTY_TX_SET

## Targets

- /Users/siddharthsuresh/dev/stellar-horizon
- /Users/siddharthsuresh/dev/stellar-rpc

## Notes — read carefully before planning

### Base branch + PR conventions

- **Base branch for both targets: `protocol-next`.** Open PRs against
  `protocol-next`, NOT `main` (horizon defaults to `main`; rpc defaults
  to `main`). Confirm per-repo with
  `gh api repos/stellar/<repo> --jq .default_branch` only as a sanity
  check — the operator-mandated base for this release is `protocol-next`
  regardless of the default.
- PR branch name on the fork: `p28-cap-0083` (same name in both repos
  for easy cross-linking).
- Status: **draft**. Mark ready only when the upstream `go-stellar-sdk`
  dep is on a merged commit — until then this is a SPIKE.

### Upstream state — what already exists; don't re-do

- **`stellar-core`** already implements CAP-0083. Do NOT open a
  stellar-core PR from this run. Its current pin on `protocol-next` is
  correct.
- **`rs-soroban-env`** does NOT need updating for CAP-0083. CAP-83 is a
  consensus-layer feature, not a host-layer one. Keep the existing
  rs-soroban-env pin in `stellar-rpc/Cargo.toml` unchanged. If the
  planner proposes a bump, reject it in review.
- **`rs-stellar-xdr`** — check the current pin first. If CAP-0083 types
  (`STELLAR_VALUE_EMPTY_TX_SET`, etc.) are already generated at that
  pin, do NOT bump. If not, a bump IS required and the planner should
  surface it explicitly. Verify with the lessons.md `gh api ...
  compare/...` recipe — don't trust the crate's self-reported version.
- **`go-stellar-sdk`** is the likely upstream that needs work. Horizon
  and rpc both pin `go-stellar-sdk` via `replace` in `go.mod`. For
  CAP-0083 types to flow into horizon/rpc's vocabulary, the SDK needs
  either:
    1. An existing `protocol-next` branch with CAP-0083 regen'd (no PR
       needed; just bump the replace SHA in horizon/rpc), OR
    2. A SPIKE PR opened on the SDK's fork as part of this run (then
       horizon/rpc `replace` directives point at its pseudo-version).
  Discover which case applies. If option 2, open the go-stellar-sdk
  upstream PR from the watch-phase fix when horizon/rpc CI surfaces a
  missing CAP-0083 type. Watch out for the `XDR_FEATURES` list in the
  SDK's Makefile — CAP_0083 needs to be present (cf. lessons.md
  go-stellar-sdk section).

### Pins that must NOT change

- `rs-soroban-env` (rpc workspace dep).
- Any unrelated Go module bumps in either repo.


### PR title pattern

- horizon: `Protocol 28 (CAP-0083)`
- rpc:     `Protocol 28 (CAP-0083)`
- Cross-link the two PRs in each description. If a `go-stellar-sdk`
  SPIKE PR gets opened, cross-link that too.

### MaxSupportedProtocolVersion bumps

Per the lessons file:
- horizon: bump `MaxSupportedProtocolVersion` in
  `internal/ingest/main.go`.
- rpc: do NOT touch the `MaxSupportedProtocolVersion` const in
  `cmd/stellar-rpc/internal/integrationtest/...` — it's not part of the
  protocol-bump pattern.
